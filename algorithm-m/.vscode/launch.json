{
    "version": "0.2.0",
    "configurations": [
        {
            "command": "npm test -- --watch",
            "name": "Debug tests (watch)",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}